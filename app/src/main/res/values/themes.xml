<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NeoQuizApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NeoQuizApp" parent="Base.Theme.NeoQuizApp" />

    <style name="EditText">
        <item name="android:paddingStart">15dp</item>
        <item name="android:textColor">@color/et_text</item>
        <item name="android:background">@drawable/rounded_et</item>
    </style>

    <style name="AppTheme.SearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/black</item>
        <item name="android:background">@drawable/rounded_et</item>
    </style>

</resources>